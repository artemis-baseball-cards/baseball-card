<!DOCTYPE html>
<html>
  <head>
    <title>BaseballCard</title>
    <%= javascript_include_tag "application", "chartkick" %>
    <%= javascript_include_tag "https://www.google.come/jsapi" %>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <%= csrf_meta_tags %>
    <% if content_for?(:head) %>
      <%= yield(:head) %>
    <% end %>
  </head>

  <body>
    <div class="site-content">
      <nav class="navbar">
        <div class="container-fluid">
          <div class="navbar-header">
            <a class="navbar-brand" href="/" id="logo">Artemis Cards</a>
          </div>
          <ul class="nav navbar-nav navbar-right">
            <% if current_user %>
              <% if current_user.superadmin_role? || current_user.supervisor_role? %>
                <li class="regular-nav-link"><%= link_to 'ADMIN', rails_admin_path %></li>
              <% end %>
              <li class="regular-nav-link"><%= link_to 'PROJECTS', projects_path %></li>
              <li class="regular-nav-link"><%= link_to 'EMPLOYEES', employees_path %></li>
              <% if current_user.superadmin_role? || current_user.supervisor_role? %>
                <li class="regular-nav-link"><%= link_to 'CLIENTS', companies_path %></li>
              <% end %>
              <li class="regular-nav-link"><%= link_to 'SIGN OUT', destroy_user_session_path, :method => :delete %></li>
              <li id="small"><%= current_user.email %></li>
            <% else %>
              <li><%= link_to 'SIGN IN', new_user_session_path %></li>
              <li><%= link_to 'SIGN Up', new_user_registration_path %></li>
            <% end %>
          </ul>
        </div>
      </nav>
      <%= yield %>
    </div>

    <footer class="page-footer center-on-small-only">
      <div class="container-fluid">
        <div class="row">
          <div class="container">
            <p>STRATEGY FOR INNOVATIVE LEADERS</p>
          </div>
        </div>
      </div>
      <div class="footer-copyright">
        <div class="container-fluid">
          © 2018 <a href="https://www.artemisconnection.com/">Artemis Connection, Inc.</a> | All rights reserved.
        </div>
      </div>
    </footer>
  </body>


</html>
